---
_metadata:
  name: Role Config Context Role 1
  weight: 500
  description: Filtered on Role 1 via Dynamic Group
  is_active: true
  dynamic_groups:
    - slug: dynamic-group-role-1

type: "context applied via role 1 in a dynamic group"
